import styled from "styled-components";
import LinearProgress from '@material-ui/core/LinearProgress';

export const CostsBar = styled(LinearProgress)`
`

export const IncomeBar = styled(LinearProgress)`
`
