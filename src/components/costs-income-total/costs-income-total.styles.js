import styled from "styled-components";
import LinearProgress from '@material-ui/core/LinearProgress';

export const BlueBar = styled(LinearProgress)`
`

export const GreyBar = styled(LinearProgress)`
`
